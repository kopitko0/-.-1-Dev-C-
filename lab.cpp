#include <iostream>
using namespace std; 

int main() {
    float a, b, S, P;
    cin >> a;
    cin >> b;
    S = a + b;
    cout << "a + b =" << S;
    cin >> a;
    cin >> b;
    P = 2 * (a + b);
    cout << "2 * ( a + b )" << P;
    return 0;

}
